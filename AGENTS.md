# 项目级代理约定

## 配置驱动原则

- 对可能随内容运营、视觉调优、交互节奏或部署环境变化的值，优先使用配置文件，不在 React 组件或业务逻辑中硬编码。
- 可公开且需要在浏览器运行时读取的配置放入 `public/config/*.properties`；环境差异配置可使用 `.env`。密钥、令牌等敏感信息禁止放入 `public` 或以 `VITE_` 开头的客户端环境变量。
- 三月七语录及 typed.js 动效参数统一维护在 `public/config/quotes.properties`。新增语录使用 `quote.<序号>`，动效参数使用 `typed.*`，不得在组件中重复定义同一份值。
- 新增或调整配置项时，必须同步补充配置注释、解析逻辑、类型约束与非法值处理；必填配置缺失时应给出包含配置键名的明确错误信息。
- 固定的页面结构、类型约束和配置键名可以保留在代码中；只有具备实际变化可能的内容与参数需要配置化，避免为了配置化而过度设计。
- 完成配置变更后，应验证开发环境读取、生产构建复制和异常配置处理，确保配置文件与构建产物内容一致。

## Repository Knowledge

- 人类阅读入口：[Repo Wiki](docs/repo-wiki/index.md)，负责项目认知、架构与源码导航。
- Agent 持久知识入口：[Knowledge Index](.knowledge/index.md)，高密度知识位于 `.knowledge/cards/`。
- 源码、测试、配置和实际运行行为是最终事实来源。文档冲突时先核实实现，再修正文档，不让正确代码迁就过时说明。
- `RepoWiki.md` 是知识体系初始化参考；历史分析和提示词不代表已经实现的功能，也不替代本文件的工程规则。

## Context Loading Protocol

简单、局部、低风险修改可以直接检查相关源码。非平凡任务按以下顺序加载上下文：

1. 阅读本文件，识别任务涉及的模块、领域或流程。
2. 不熟悉项目时先读 Wiki 首页；按任务关键词搜索卡片，例如 `rg -n -i 'hydration|路由' .knowledge/cards`。
3. 只读取相关的 1～3 张卡片，必要时跟随链接阅读模块或流程说明，不全量读取卡片。
4. 根据 Wiki / Card 的 source 导航检查真实源码、配置和相关测试，确认当前行为后再修改。

不要为每个任务盲目扫描整个仓库。配置、滚动、相册、构建/路由的任务入口见两个知识索引。

## Development Principles

- 修改前确认现有实现；优先最小、有针对性的改动，避免无关重构。
- 复用已有抽象，尊重模块边界，不为方便实现放宽业务约束。
- 不无理由增加依赖，不删除已有行为来简化任务。
- 新增或调整代码时补齐必要注释，保留仍有效的旧注释。
- Git 提交备注与 PR 默认使用简体中文；在主分支新增功能前先切换到按任务命名的 `codex/` 分支。

## Knowledge Maintenance

任务闭环：用户需求 → 相关 Wiki / Card → 当前源码验证 → 实现 → 测试/验证 → 知识影响检查 → 必要的文档同步。

非平凡修改完成前，检查是否改变架构、模块职责或边界、公开 API、数据模型、重要流程、业务不变量、技术约束、跨模块依赖、架构决策或已知易错点。发生变化时，同步更新受影响的 Wiki 和 Card；两种视图描述同一事实，优先更新已有条目，并核对 source 路径及相互链接。

简单 helper、单文件可直接理解的细节、临时调试和短期任务状态不进入长期知识。仅在真实错误或重复探索暴露知识缺口时扩充，不按文件数量追求覆盖率。持久知识变化的同步维护属于任务完成条件。
